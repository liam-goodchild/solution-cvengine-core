# solution-cvengine-portfolio

Serverless portfolio site on Azure: a static frontend on Azure Static Web Apps, a Node.js Azure Function that tracks visitor counts in Cosmos DB, and Terraform IaC that provisions the full stack with a custom domain via DNS.
